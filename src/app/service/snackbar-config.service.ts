import { MatSnackBarConfig } from '@angular/material/snack-bar';

export const commonSnackBarConfig: MatSnackBarConfig = {
  verticalPosition: 'top',
  horizontalPosition: 'center',
  duration: 3000,
};
